<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f4f7fc;
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      text-align: center;
    }
    .container {
      background-color: #fff;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      width: 100%;
    }
    h1 {
      font-size: 2rem;
      margin-bottom: 20px;
      color: #4CAF50;
    }
    .form-container {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }
    input[type="text"] {
      padding: 12px;
      border: 1px solid #ccc;
      border-radius: 5px;
      font-size: 1rem;
      width: 100%;
      max-width: 500px;
      margin: 0 auto;
    }
    button {
      padding: 12px;
      background-color: #4CAF50;
      border: none;
      color: #fff;
      font-size: 1rem;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      width: 100%;
      max-width: 500px;
      margin: 0 auto;
    }
    button:hover {
      background-color: #45a049;
    }
    .result {
      margin-top: 20px;
    }
    .result p {
      font-size: 1rem;
      word-wrap: break-word;
    }
    .error {
      color: #e74c3c;
      font-weight: bold;
    }
    .success {
      color: #2ecc71;
      font-weight: bold;
    }
    .link-container {
      margin-top: 10px;
      font-size: 1.1rem;
    }
    .link-container a {
      color: #3498db;
      text-decoration: none;
      word-wrap: break-word;
      word-break: break-all;
    }
    .link-container a:hover {
      text-decoration: underline;
    }
    .footer {
      margin-top: 30px;
      font-size: 0.9rem;
      color: #999;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>URL Shortener</h1>
    
    <!-- Show error message -->
    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <!-- Show short URL -->
    <% if (shortUrl) { %>
      <div class="result success">
        <p>Your short URL is:</p>
        <div class="link-container">
          <a href="<%= shortUrl %>" target="_blank"><%= shortUrl %></a>
        </div>
      </div>
    <% } %>

    <!-- URL shortening form -->
    <form action="/shorten" method="POST" class="form-container">
      <input type="text" name="longUrl" placeholder="Enter URL to shorten" required />
      <button type="submit">Shorten</button>
    </form>
    
    <div class="footer">
      <p>URL Shortener Service | Designed by <a href="https://www.linkedin.com/in/rajendra-nath-b456bb302/" target="_blank">Rajen M</a></p>
    </div>
  </div>
</body>
</html>
